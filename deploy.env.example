# Copy to deploy.env and set for your production server.
# DEPLOY_HOST=10.10.10.64
# DEPLOY_USER=root
# DEPLOY_PATH=/local/bin/projects/Cybersecurity
#
# REQUIRED in production: JWT_SECRET (min 32 chars, random string)
# JWT_SECRET=your-random-secret-at-least-32-characters-long
#
# Google OAuth (optional). User must already exist in db.json with the same email.
# Store client secret securely (e.g. Google Secret Manager); never commit real values to git.
# https://developers.google.com/identity/protocols/oauth2/resources/best-practices
# In Google Cloud Console: OAuth 2.0 Client (Web), add authorized redirect URI exactly:
#   https://cod-data.com/api/auth/google/callback   (or http://localhost:5010/api/auth/google/callback for dev)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_OAUTH_REDIRECT_URI=https://cod-data.com/api/auth/google/callback
# FRONTEND_ORIGIN=https://cod-data.com
# Optional: restrict sign-in to one email domain (e.g. yourcompany.com)
# GOOGLE_ALLOWED_EMAIL_DOMAIN=
#
# Set to true to disable email/password login and open (non-invite) registration; use Google OAuth.
# Auditors can still create users without passwords; invitation links can complete with optional password.
# Use plain true/1/yes/on (case-insensitive). File must live next to ecosystem: .../Cybersecurity/deploy.env
# GOOGLE_AUTH_ONLY=true
#
# Verify after pm2 restart: logs should contain "GOOGLE_AUTH_ONLY=true".
# curl -sS https://cod-data.com/api/auth/google/enabled  → passwordLogin:false when enabled.
